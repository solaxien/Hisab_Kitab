# Module-specific build directory
/build

# Release artifacts (if you store them in the app folder)
/release/*.apk
/release/*.aab

# If you use Google Services, you might want to ignore the config 
# if you keep a template and don't want to leak your actual API keys.
# google-services.json